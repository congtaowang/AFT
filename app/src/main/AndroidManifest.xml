<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.aft.sample">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".AFTApplicatioin"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SimpleAdapterActivity" />
        <activity android:name=".ui.SimpleFrescoImageViewsActivity" />
        <activity android:name=".ui.AlbumActivity" />
        <activity android:name=".ui.pattern.PatternsActivity" />
        <activity android:name=".ui.pattern.ChainOfResponsibilityPatternActivity" />
        <activity android:name=".ui.CardViewActivity" />
        <activity android:name=".ui.TakePhotoActivity"/>
    </application>

</manifest>
